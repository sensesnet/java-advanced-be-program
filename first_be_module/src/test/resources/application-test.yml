spring:
  application:
    name: first-be-module
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: username
    password: password
  h2:
    console:
      enabled: true
      path: /h2-ui
  jpa:
    show-sql: true

server:
  port: 8081

custom.datasource.enabled: true

